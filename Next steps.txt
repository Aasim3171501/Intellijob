You have full green-light approval to execute Step 1 and Step 2 on our current feature branch `feature/phase-b2-skill-matching-api`.

Here are the requirements for this turn:

1. Requirements Update:
   - Update `intellijob-backend/requirements.txt` to pin our newly installed dependencies (`torch`, `sentence-transformers`, `transformers`, `huggingface-hub`, `spacy`, `pymupdf4llm`).

2. Skill Extraction Service (`api/services/skill_extractor.py`):
   - Use PyMuPDF / `pymupdf4llm` to extract clean text from uploaded PDF resumes.
   - Use `spaCy` (`en_core_web_sm`) with a custom `EntityRuler` to parse technical entity tokens (`SKILL`).
   - Add unit tests in `api/tests/test_skill_extractor.py`.

3. Cosine Matching Service (`api/services/matcher.py`):
   - Build a matching service that embeds candidate skills + target job title using `SentenceTransformers`.
   - Perform fast dot-product cosine distance ranking against `data/job_embeddings.npz` (loading with `allow_pickle=True`).
   - Return the top 3–5 matched job records joined with `data/uk_software_jobs.csv`.
   - Add unit tests in `api/tests/test_matcher.py`.

4. DRF Endpoint (`POST /api/analyze/`):
   - Wire the view in `api/views.py` and route in `api/urls.py`.
   - Accept a PDF resume upload (`file`) and target title (`target_title`).
   - Return a JSON response containing: extracted candidate skills, top matched jobs with similarity scores, and a placeholder structure for the upcoming local RAG roadmap generator.
   - Add integration tests in `api/tests/test_views.py`.

Run your full test suite (`python manage.py test`) when complete to verify 100% pass status.